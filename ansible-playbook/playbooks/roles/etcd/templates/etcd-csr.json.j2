{
    "CN": "etcd",
    "hosts": [
        "127.0.0.1",
{% for host in groups['etcd'] %}
{% if not loop.last %}
        "{{ hostvars[host]['ansible_host'] }}",
{% else %}
        "{{ hostvars[host]['ansible_host'] }}"
{% endif %}
{% endfor %}
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
        {
            "C": "CN",
            "ST": "BeiJing",
            "L": "BeiJing",
            "O": "k8s",
            "OU": "System"
        }
    ]
}
