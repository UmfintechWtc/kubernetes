[Unit]
Description=Kubernetes Proxy
After=network.target

[Service]
EnvironmentFile={{ k8s_node_cfg_path }}/kube-proxy.conf
ExecStart={{ k8s_node_bin_path }}/kube-proxy $KUBE_PROXY_OPTS
Restart=on-failure
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target